{{define "content"}}
<h2>Edit {{.Journal.Title}}</h2>

{{if .Error}}
    <div class="error">Make sure all the fields are filled in before saving.</div>
{{end}}

{{template "form" .}}

<p><a href="/">Back</a></p>
{{end}}
