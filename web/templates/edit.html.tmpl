{{define "title"}}Edit {{.Journal.Title}} - {{end}}
{{define "content"}}
<h1 class="form-title">Edit {{.Journal.Title}}</h1>

{{if .Error}}
    <div class="error">Make sure all the fields are filled in before saving.</div>
{{end}}

{{template "form" .}}
{{end}}
